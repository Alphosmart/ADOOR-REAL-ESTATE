services:
  - type: web
    name: adoo-real-estate-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /api/*
        destination: https://adoorealestate.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html